[project]
name = "steve-devops-api"
version = "0.1.0"
description = "A FastAPI-based DevSecOps operational API learning project."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
