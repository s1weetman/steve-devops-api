apiVersion: v1
kind: Service
metadata:
  name: steve-devops-api-service
  labels:
    app: steve-devops-api
spec:
  type: NodePort
  selector:
    app: steve-devops-api
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
